import { ShortUrlPipePipe } from './short-url-pipe.pipe';

describe('ShortUrlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortUrlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
